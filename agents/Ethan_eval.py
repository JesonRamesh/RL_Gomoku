def evaluate(num_games=100):
    """
    Load trained checkpoint, set epsilon=0, and run fixed number of games.

    Report:
        - win/draw/loss counts
        - average reward
        - optional move count / invalid-action count
    """
